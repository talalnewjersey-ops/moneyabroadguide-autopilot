name: MAG Autopilot Agent

on:
  workflow_dispatch:

jobs:
  seo_agent:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch posts from WordPress
        run: |
          curl -L \
          -A "Mozilla/5.0" \
          "https://moneyabroadguide.com/wp-json/mag/v1/posts"
